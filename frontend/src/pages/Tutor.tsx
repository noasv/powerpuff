import {useState} from 'react';import {Send,Sparkles} from 'lucide-react';import Layout from '../components/Layout';import {api} from '../services/api';
export default function Tutor(){const [messages,setMessages]=useState([{who:'ai',text:'Tell me which part of your reasoning feels least certain. I will ask questions before giving an explanation.'}]);const [text,setText]=useState('');const [step,setStep]=useState(1);const [busy,setBusy]=useState(false);async function send(e:React.FormEvent){e.preventDefault();if(!text)return;const value=text;setMessages(m=>[...m,{who:'you',text:value}]);setText('');setBusy(true);try{const r=await api<{message:string}>('/api/ai/tutor',{method:'POST',body:JSON.stringify({concept_id:1,message:value,step})});setMessages(m=>[...m,{who:'ai',text:r.message}]);setStep(s=>Math.min(8,s+1))}finally{setBusy(false)}}return <Layout><div className="tutor"><header><span><Sparkles/></span><div><p className="eyebrow">SOCRATIC SUPPORT</p><h1>AI Tutor</h1><p>Guidance without immediately revealing the answer.</p></div></header><div className="chat">{messages.map(m=><div className={`message ${m.who}`}><small>{m.who==='ai'?'CALIBRATE AI':'YOU'}</small><p>{m.text}</p></div>)}{busy&&<div className="message ai"><p>AI Tutor is preparing the next question…</p></div>}</div><form onSubmit={send}><label htmlFor="tutor-input">Your reasoning</label><textarea id="tutor-input" value={text} onChange={e=>setText(e.target.value)} placeholder="Explain what you think happens and why…"/><button className="button" disabled={busy||!text}>Send <Send/></button></form></div></Layout>}
