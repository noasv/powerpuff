export const pct=(v:number)=>`${Math.round(v*100)}%`;export function Badge({status}:{status:string}){return <span className={`badge ${status.toLowerCase().replaceAll(' ','-')}`}>{status}</span>}export function Loading({label='Loading your evidence…'}:{label?:string}){return <div className="loading"><span className="spinner"/>{label}</div>}export function ErrorBox({message,retry}:{message:string;retry?:()=>void}){return <div className="error"><b>Something went wrong</b><p>{message}</p>{retry&&<button onClick={retry}>Try again</button>}</div>}export function Empty({text}:{text:string}){return <div className="empty">{text}</div>}
